<Window x:Class="Bank.Loading"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bank"
        mc:Ignorable="d"
        Title="Loading" Height="200" Width="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
           Loaded="Window_Loaded" >
    <Grid>
        <local:UcSimpleBorder/>
        <TextBlock Opacity="0.4" x:Name="TextConst" FontFamily="IRANSansWeb" FontSize="36"  VerticalAlignment="Center" TextAlignment="Center"   Text="  نرم افزار درحال لود شدن است" >
            <TextBlock.Effect>

                <BlurEffect x:Name="blref" Radius="15" />
            </TextBlock.Effect>
        </TextBlock>
    </Grid>
</Window>
